# Backend URL — set this to your Render backend URL in production
VITE_API_URL=https://sanjeevni-backend.onrender.com

# For local development, comment out above and leave unset (defaults to http://localhost:3125)
